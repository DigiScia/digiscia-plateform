# fly.toml
app = "digiscia-backend"
primary_region = "cdg"

[build]
  dockerfile = "Dockerfile"

[env]
  DJANGO_SETTINGS_MODULE = "backend.settings"
  PORT = "8080"

# ----------------------------------------------------------
# 🚀 COMMANDE DE DÉPLOIEMENT (Migration Auto)
# ----------------------------------------------------------
# Cette commande s'exécute dans une VM temporaire avant le déploiement final.
# Si elle échoue, le déploiement est annulé (sécurité).
[deploy]
  release_command = "python manage.py migrate"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1